/// Deployment-declared shield geometry.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub(crate) struct ShieldTopology {
    _priv: (),
}

/// Gate placement at points-of-effect.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub(crate) struct GatePlacement {
    _priv: (),
}

/// Finite admissible action set.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub(crate) struct AdmissibleSet {
    _priv: (),
}
